export const IS_MOBILE = navigator.userAgent.toLocaleLowerCase().includes('ios') || navigator.userAgent.toLocaleLowerCase().includes('android');

export const SIZES = [
  [24, 24],
  [48, 48],
  [96, 96],
  [126, 126],
  [256, 256],
]